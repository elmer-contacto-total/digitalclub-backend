# Development Profile Configuration
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:postgresql://digital-club-test.clbzi5ldkemb.us-east-1.rds.amazonaws.com:5432/digital_club_staging
    username: postgres
    password: Digitalgroup1!

  # Flyway migrations enabled for development
  flyway:
    enabled: true

# Universal password para desarrollo (bypass password check)
app:
  universal-password: test123

logging:
  level:
    com.digitalgroup.holape: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.web: DEBUG

# Development-specific JWT (shorter secret for dev)
jwt:
  secret: dev-secret-key-minimum-32-characters-long
  expiration: 86400000
